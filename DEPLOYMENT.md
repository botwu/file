# GitHub Pages 部署指南

## 📋 部署步骤

### 1. 推送代码到 GitHub

首先，确保你的项目已经推送到 GitHub 仓库：

```bash
git add .
git commit -m "配置 GitHub Pages 部署"
git push origin main
```

### 2. 启用 GitHub Pages

1. 打开你的 GitHub 仓库页面
2. 点击 **Settings**（设置）标签
3. 在左侧菜单中找到 **Pages**
4. 在 **Source**（源）部分：
   - 选择 **GitHub Actions** 作为部署源
   
![GitHub Pages 设置](https://docs.github.com/assets/cb-166472/mw-1440/images/help/pages/publishing-source-drop-down.webp)

### 3. 自动部署

配置完成后，每次推送到 `main` 分支时，GitHub Actions 会自动：
- 安装依赖
- 构建项目
- 部署到 GitHub Pages

你可以在仓库的 **Actions** 标签页查看部署进度。

### 4. 访问你的网站

部署成功后，你的网站将在以下地址可访问：

```
https://你的用户名.github.io/仓库名/
```

例如：`https://wujian.github.io/cute-portfolio/`

## 🔧 配置说明

### 文件变更

1. **vite.config.ts** - 添加了 `base: './'` 配置，使用相对路径
2. **.github/workflows/deploy.yml** - GitHub Actions 自动部署工作流
3. **.gitignore** - 忽略构建文件和依赖
4. **package.json** - 添加了 `predeploy` 脚本

### 手动触发部署

如果需要手动触发部署：
1. 进入仓库的 **Actions** 标签
2. 点击左侧的 "部署到 GitHub Pages" 工作流
3. 点击 **Run workflow** 按钮
4. 选择分支并运行

## 🌟 注意事项

- 首次部署可能需要几分钟时间
- 确保仓库是公开的（public），或者你有 GitHub Pro 账户（私有仓库也支持 Pages）
- 如果使用了环境变量（如 API Key），需要在 GitHub Settings > Secrets 中配置
- 每次推送到 main 分支都会触发自动部署

## 🚀 本地测试

在部署前，建议先本地测试构建：

```bash
# 构建项目
npm run build

# 预览构建结果
npm run preview
```

## ❓ 常见问题

### 页面显示 404
- 检查 GitHub Pages 设置是否正确启用
- 确认工作流是否成功运行
- 等待几分钟让 DNS 生效

### 资源加载失败
- 检查 `vite.config.ts` 中的 `base` 配置
- 确保使用相对路径或正确的仓库名

### 部署失败
- 查看 Actions 标签中的错误日志
- 检查 `package.json` 中的依赖是否正确
- 确保有足够的 GitHub Actions 运行时间配额

## 📚 更多资源

- [GitHub Pages 文档](https://docs.github.com/zh/pages)
- [GitHub Actions 文档](https://docs.github.com/zh/actions)
- [Vite 部署指南](https://vitejs.dev/guide/static-deploy.html)

